# Unit 7 — Frontend cutover (Implementation Steps)

You are the Implementation agent for Unit 7. Zero memory.

**Working directory:** `/Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier`

## What you're doing

Units 1-6 moved Greenhouse scraping to the backend. Greenhouse jobs now land in `job_listings` via Procrastinate cron and are served by `GET /api/jobs?company=<id>`.

Your job: flip 45 Greenhouse entries in `companies.ts` to `createBackendScraperCompany`, delete now-dead Greenhouse-client code, update routing/config, fix tests, update two CLAUDE.md files.

---

## Pre-flight discovery

```bash
# 1. Confirm 45 Greenhouse entries
grep -c "createGreenhouseCompany(" /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/config/companies.ts
# Expected: 46 (45 entries + 1 function definition)

# 2. List entries
grep -E "createGreenhouseCompany\(" /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/config/companies.ts \
  | grep -v "^function" \
  | sed -E "s/.*createGreenhouseCompany\('([^']+)', *'([^']+)'.*/\1 | \2/"

# 3. Find every file referencing dead Greenhouse code
find /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier -type f \( -name "*.ts" -o -name "*.tsx" \) \
  -not -path "*/node_modules/*" -not -path "*/dist/*" -not -path "*/.git/*" -not -path "*/.claude/worktrees/*" \
  | xargs grep -l -E "(greenhouseTransformer|greenhouseClient|api/greenhouse|/api/greenhouse|transformGreenhouseJob)" 2>/dev/null

# 4. Confirm createBackendScraperCompany signature
grep -A 6 "^function createBackendScraperCompany" /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/config/companies.ts | head -10
# Expected: (id: string, name: string, jobsUrl: string, options: FactoryOptions = {})

# 5. Backend Unit 3 emits the right keys
grep -nE "experience_level|is_remote_eligible" /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/backend/api/services/greenhouse_client.py
```

---

## Section A — Edit `companies.ts`

### A.1 — Flip all 45 Greenhouse entries

Use the pattern:
```diff
- createGreenhouseCompany('foo', 'Foo'),
+ createBackendScraperCompany('foo', 'Foo', 'https://boards.greenhouse.io/foo'),
```

With options:
```diff
- createGreenhouseCompany('foo', 'Foo', { recruiterLinkedInUrl: '...' }),
+ createBackendScraperCompany('foo', 'Foo', 'https://boards.greenhouse.io/foo', { recruiterLinkedInUrl: '...' }),
```

The 45 entries: spacex, andurilindustries, airtable, airbnb, fireworksai, figma, twitch, neuralink, robinhood, xai, anthropic, reddit, cloudflare, scaleai, lyft, doordashusa, stripe, appliedintuition, discord, brex, squarespace, clear, affirm, crunchyroll, nuro, pallet, pinterest, astranis, waymo, figureai, gleanwork, merge, databricks, datadog, dropbox, instacart, mongodb, twilio, block, gitlab, unity3d, vercel, thinkingmachines, togetherai, hightouch.

**Don't touch** `createAshbyCompany('chalk', ...)` which sits in the middle of the Greenhouse block.

### A.2 — Clean up unused factory + type import

- Remove the `createGreenhouseCompany` function definition (~lines 31-49) and `GreenhouseOptions` interface (~lines 19-21).
- Remove `GreenhouseConfig` from the type import block at top of file.
- Rename or remove the `// Greenhouse companies` comment (recommended: rename to `// Backend scraper companies (formerly Greenhouse)`).

### A.3 — Verify

```bash
grep -n "createGreenhouseCompany\|GreenhouseConfig" /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/config/companies.ts
# Expected: no output
```

---

## Section B — Delete dead client code

### B.1 — Pre-deletion safety check

```bash
grep -rn -E "from .*greenhouseClient|from .*greenhouseTransformer" \
  /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src \
  --include="*.ts" --include="*.tsx" \
  | grep -v "__tests__" | grep -v "greenhouseClient.ts\|greenhouseTransformer.ts"
```

After Section A, the only remaining importer is `src/frontend/src/api/utils.ts` (fixed in B.4).

### B.2 — Delete frontend transformer

```bash
rm /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/api/transformers/greenhouseTransformer.ts
```

### B.3 — Delete frontend client

```bash
rm /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/api/clients/greenhouseClient.ts
```

### B.4 — Edit `src/frontend/src/api/utils.ts`

```diff
 import type { JobAPIClient } from './types';
-import { greenhouseClient } from './clients/greenhouseClient';
 import { leverClient } from './clients/leverClient';
 ...
 export function getClientForATS(atsType: string): JobAPIClient {
   switch (atsType) {
-    case 'greenhouse':
-      return greenhouseClient;
     case 'lever':
       return leverClient;
```

**Do NOT** remove `'greenhouse'` from `ATSProvider` union or `ATSConstants` — `appSlice.ts` still references them as defaults. Pruning is out of scope.

### B.5 — Edit JSDoc example in `src/frontend/src/api/clients/baseClient.ts`

The `@example` block uses Greenhouse. Replace with a Lever example:

```diff
- * @example
- * ```typescript
- * // Creating a Greenhouse client (~15 lines vs 74 lines before)
- * export const greenhouseClient = createAPIClient<...>({
- *   ...
- * });
- * ```
+ * @example
+ * ```typescript
+ * // Creating a Lever client (~15 lines vs 74 lines before)
+ * export const leverClient = createAPIClient<LeverAPIResponse, LeverConfig>({
+ *   name: 'Lever',
+ *   buildUrl: (config) => `${config.apiBaseUrl}/v0/postings/${config.companyId}`,
+ *   extractJobs: (response) => response,
+ *   transformer: transformLeverJob,
+ *   getIdentifier: (config) => config.companyId,
+ *   validateConfig: (config): config is LeverConfig =>
+ *     config.type === 'lever',
+ * });
+ * ```
```

### B.6 — Delete the Vercel serverless function

```bash
rm /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/api/greenhouse.ts
```

**Only** after Section A is complete.

---

## Section C — Update routing config

### C.1 — `vercel.json`: remove the Greenhouse rewrite

```diff
   "rewrites": [
-    {
-      "source": "/api/greenhouse/:path(.*)",
-      "destination": "/api/greenhouse?path=:path"
-    },
     {
       "source": "/api/lever/:path(.*)",
```

### C.2 — `src/frontend/vite.config.ts`: remove Greenhouse dev proxy

```diff
     proxy: {
       '/api/jobs': {
         target: 'http://localhost:8000',
         ...
       },
-      '/api/greenhouse': {
-        target: 'https://boards-api.greenhouse.io',
-        changeOrigin: true,
-        rewrite: (path) => path.replace(/^\/api\/greenhouse/, ''),
-        secure: false,
-      },
       '/api/lever': {
```

### C.3 — No `vercel.ts` exists. Skip.

---

## Section D — Test file dispositions

### D.1 — DELETE `src/frontend/src/__tests__/api/transformers/greenhouseTransformer.test.ts`

```bash
rm /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/__tests__/api/transformers/greenhouseTransformer.test.ts
```

### D.2 — DELETE `src/frontend/src/__tests__/api/serverless/greenhouse.serverless.test.ts`

```bash
rm /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src/__tests__/api/serverless/greenhouse.serverless.test.ts
```

### D.3 — UPDATE `src/frontend/src/__tests__/api/utils.test.ts`

```diff
 import { describe, it, expect } from 'vitest';
 import { getClientForATS } from '../../api/utils';
-import { greenhouseClient } from '../../api/clients/greenhouseClient';
 import { leverClient } from '../../api/clients/leverClient';
 ...

 describe('getClientForATS', () => {
-  it('returns greenhouseClient for greenhouse ATS type', () => {
-    const client = getClientForATS('greenhouse');
-    expect(client).toBe(greenhouseClient);
-  });

   it('returns leverClient for lever ATS type', () => {
```

### D.4 — LEAVE ALONE `src/frontend/src/__tests__/api/baseClient.test.ts`

Uses `GreenhouseConfig` as a generic stand-in for the factory. Doesn't depend on the deleted client/transformer.

### D.5 — UPDATE `src/frontend/src/__tests__/app/App.test.tsx`

Replace Greenhouse-shape mock data with backend-shape mock data:

```diff
-const mockGreenhouseJobs = {
-  jobs: [
-    {
-      id: 1,
-      title: 'Senior Software Engineer',
-      absolute_url: 'https://spacex.com/careers/1',
-      location: { name: 'Hawthorne, CA' },
-      departments: [{ id: 1, name: 'Engineering' }],
-      offices: [],
-      updated_at: new Date(Date.now() - 1000 * 60 * 30).toISOString(),
-    },
-  ],
-};
+const mockBackendJobs = [
+  {
+    id: 'greenhouse_spacex_1',
+    title: 'Senior Software Engineer',
+    company: 'spacex',
+    location: 'Hawthorne, CA',
+    url: 'https://spacex.com/careers/1',
+    sourceId: 'greenhouse_api',
+    details: JSON.stringify({ experience_level: null, is_remote_eligible: false }),
+    createdAt: new Date(Date.now() - 1000 * 60 * 30).toISOString(),
+    postedOn: new Date(Date.now() - 1000 * 60 * 30).toISOString(),
+    closedOn: null,
+    status: 'OPEN',
+    hasMatched: false,
+    aiMetadata: '{}',
+    firstSeenAt: new Date(Date.now() - 1000 * 60 * 30).toISOString(),
+    lastSeenAt: new Date(Date.now() - 1000 * 60 * 30).toISOString(),
+    consecutiveMisses: 0,
+    detailsScraped: true,
+  },
+];

 const server = setupServer(
-  http.get('/api/greenhouse/v1/boards/*/jobs', () => {
-    return HttpResponse.json(mockGreenhouseJobs);
+  http.get('/api/jobs', () => {
+    return HttpResponse.json(mockBackendJobs);
   }),
```

Also update every other `http.get('/api/greenhouse/v1/boards/*/jobs', ...)` in the file (the Error Handling describe block) — swap URL to `/api/jobs` and any `mockGreenhouseJobs` → `mockBackendJobs`. The `HttpResponse.error()` ones just need URL swap.

After: `grep -n "/api/greenhouse" src/frontend/src/__tests__/app/App.test.tsx` should be empty.

### D.6 — UPDATE `src/frontend/src/__tests__/integration/fullWorkflow.test.tsx`

Same pattern — swap mock shape and URL. Keep the test intent (multiple jobs).

```diff
-  http.get('/api/greenhouse/v1/boards/spacex/jobs', ({ request }) => {
-    const url = new URL(request.url);
-    if (url.searchParams.get('content') === 'true') {
-      return HttpResponse.json(mockGreenhouseJobs);
-    }
-    return HttpResponse.json({ jobs: [] });
-  }),
+  http.get('/api/jobs', ({ request }) => {
+    const url = new URL(request.url);
+    if (url.searchParams.get('company') === 'spacex') {
+      return HttpResponse.json(mockBackendJobs);
+    }
+    return HttpResponse.json([]);
+  }),
```

### D.7 — UPDATE `src/frontend/src/__tests__/app/AppEnabledCompaniesGlobalLoad.test.tsx`

```diff
-  http.get('/api/greenhouse/v1/boards/*/jobs', () => HttpResponse.json({ jobs: [] })),
   http.get('/api/ashby/v1/jobBoard/:boardName/jobs', () =>
     HttpResponse.json({ jobs: [] })
   ),
   http.get('/api/lever/v0/postings/*', () => HttpResponse.json([])),
-  http.get('/api/jobs', () => HttpResponse.json({ jobs: [] }))
+  http.get('/api/jobs', () => HttpResponse.json([]))
 );
```

### D.8 — UPDATE `src/frontend/src/__tests__/app/hooks/useCompanyLoader.test.tsx`

Same pattern as D.5. Multiple `http.get('/api/greenhouse/v1/boards/*/jobs', ...)` blocks (setup + error-state override). Each one: URL → `/api/jobs`, mock shape → backend `BackendJobListing[]`.

After: `grep -n "/api/greenhouse" src/frontend/src/__tests__/app/hooks/useCompanyLoader.test.tsx` should be empty.

### D.9 — Final test sweep

```bash
grep -rn "/api/greenhouse\|greenhouseClient\|transformGreenhouseJob\|greenhouseTransformer" \
  /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier/src/frontend/src \
  --include="*.ts" --include="*.tsx"
# Expected: zero results
```

---

## Section E — Update CLAUDE.md files

### E.1 — Root `CLAUDE.md`

**Edit 1 — line 3:**
```diff
-... using external ATS APIs (Greenhouse, Lever, Ashby, Workday, Gem, Eightfold). Built with Redux Toolkit, ...
+... using external ATS APIs (Lever, Ashby, Workday, Gem, Eightfold) for some and the backend's `/api/jobs` endpoint for others (Greenhouse companies, Google, Apple, Microsoft). Built with Redux Toolkit, ...
```

**Edit 2 — API Clients section (~line 60):**
```diff
-Five ATS providers (Greenhouse, Lever, Ashby, Workday, Gem) use `createAPIClient` factory ...
+Four ATS providers (Lever, Ashby, Workday, Gem) use `createAPIClient` factory ...
```
And update Backend-Scraper line to mention Greenhouse-board companies.

**Edit 3 — Adding a Company section (~line 74):**
```diff
-Edit `src/frontend/src/config/companies.ts` and add company config with ATS type (greenhouse/lever/ashby/workday/gem/eightfold). System automatically uses correct client via factory pattern.
+Edit `src/frontend/src/config/companies.ts` and add company config with ATS type (lever/ashby/workday/gem/eightfold/backend-scraper). Greenhouse boards use `backend-scraper` — add the company id and `board_token` to the backend `companies` table (see `docs/implementations/greenhouseBackendMigration/PLAN.md`) and add a `createBackendScraperCompany(id, name, 'https://boards.greenhouse.io/${id}')` entry to companies.ts. System automatically uses correct client via factory pattern.
```

**Edit 4 — Vercel Serverless Functions list (~line 120):**
```diff
-- `api/greenhouse.ts` - Greenhouse API proxy
 - `api/lever.ts` - Lever API proxy
```

### E.2 — `src/frontend/CLAUDE.md`

Same pattern — drop Greenhouse from external ATS list, update Backend-Scraper description, drop greenhouse from factory list, drop `greenhouse.ts` from serverless list.

---

## Section F — Verification

```bash
# F.1 — TypeScript clean
npm run type-check

# F.2 — All frontend tests pass
npm test -w src/frontend

# F.3 — Lint clean
npm run lint

# F.4 — Final dead-code sweep
grep -rn -E "greenhouseClient|transformGreenhouseJob|greenhouseTransformer|/api/greenhouse" \
  /Users/brendanpotter/Documents/develop/Job-Visualizer-Notifier \
  --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" \
  | grep -v "/.claude/worktrees/" \
  | grep -v "/.git/" \
  | grep -v "/node_modules/" \
  | grep -v "/dist/"
# Expected: zero, OR only matches inside docs/implementations/greenhouseBackendMigration/
```

If F.1, F.2, F.3, or F.4 fails: fix and rerun. Don't commit broken code.

---

## Pitfalls

1. **Don't delete `api/greenhouse.ts` before Section A is complete.** Order: A → B → C → D → E → F.
2. **`createBackendScraperCompany` third arg is REQUIRED** (`jobsUrl: string`). Always pass `'https://boards.greenhouse.io/${id}'`.
3. **Backend `details` payload is correct** — Unit 3 emits both keys. Don't edit `backendScraperTransformer.ts`.
4. **Backend `/api/jobs` returns a raw array**, not `{ jobs: [] }`. Critical for MSW mocks.
5. **Backend uses camelCase keys** via `alias_generator=to_camel`. Mock fields must be camelCase.
6. **Don't prune `'greenhouse'` from `ATSProvider`/`ATSConstants`.** Out of scope.
7. **`baseClient.test.ts`** is a factory test, not a client test. Don't touch.
8. **Ignore `.claude/worktrees/` paths** — sibling worktree.
9. **`appSlice.ts` `selectedATS: ATSConstants.Greenhouse` default is fine** — `useCompanyLoader` overwrites on mount.
10. **The `// Greenhouse companies` comment in companies.ts** — rename for git-blame clarity.

---

## Commit

After Section F passes:

```bash
git add -A
git commit -m "$(cat <<'EOF'
Cut over Greenhouse companies to backend scraper (Unit 7)

Flip 45 Greenhouse entries in companies.ts from createGreenhouseCompany
to createBackendScraperCompany. Now that Units 1-6 are landed, Greenhouse
jobs flow through the backend Procrastinate worker and the existing
/api/jobs endpoint, matching the Google/Apple/Microsoft pattern.

Delete dead client code (greenhouseClient.ts, greenhouseTransformer.ts,
api/greenhouse.ts) and routing (vite.config.ts and vercel.json
greenhouse entries). Update CLAUDE.md (root + frontend) and tests that
mocked /api/greenhouse/* to mock /api/jobs instead.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

If pre-commit hook fails: fix the underlying issue, create a NEW commit. Do not `--amend`.
