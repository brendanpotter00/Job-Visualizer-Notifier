{
  "name": "job-visualizer-notifier-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["src/frontend"],
  "scripts": {
    "dev": "npm run dev -w src/frontend",
    "build": "npm run build -w src/frontend",
    "test": "npm run test -w src/frontend",
    "type-check": "npm run type-check -w src/frontend",
    "lint": "npm run lint -w src/frontend",
    "format": "npm run format -w src/frontend"
  }
}
